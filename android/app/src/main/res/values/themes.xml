<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.DynamicMaterialThemeSync" parent="android:Theme.Material.Light.NoActionBar">
        <!-- This is only used as a fallback for pre-compose window; Compose provides the real theme. -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
    </style>

</resources>